[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback so client-side routes (e.g. /reports) don't 404 on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
